# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
old_backup/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
pip-wheel-metadata/

# Virtual environments
venv/
env/
ENV/
.venv/
# Nested git metadata folders (not the repo-root .gitignore / .gitattributes files)
.gitignore/
.gitattributes/

# IDE & editors
.vscode/
.idea/
*.swp
*.swo
*~
.project
.pydevproject
bestmodelplot/

# Local Peptiverse experiment (data, checkpoints, training logs, hyperopt)
peptiversedata/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Temp / logs
*.tmp
*.log
*.bak
*.orig

# Env & secrets
.env
.env.*
!.env.example

# --- AMP_PhysioChem_Predictor ---
# Keep tracked: source, app.py, CITATION.*, README, requirements, small data/*.csv as needed.
# Hugging Face Spaces reject large binaries in plain Git — use Hub, Space "Files", or Git LFS/Xet.

# Weight files: ignored by default (large binaries); tracked when under checkpoints/.
*.pt
*.pth
*.ckpt
!checkpoints/**/*.pt
!checkpoints/**/*.pth
!checkpoints/**/*.ckpt
output/
images/
data/dataanalysis/

# Full training runs (verbose logs — keep out of git unless you need them)
training_logs/

# Best-parameter JSON dumps (local tuning / search — not required for inference UI)
best_parameters/

# Training / ablation / plotting (not required for app.py or run_AMP_PhysioChem_Predictor.py)
scripts/hybrid_physchem_matrix_utils.py
scripts/plot_learning_curves_individual.py
scripts/plot_top3_model_predictions.py
scripts/run_component_ablation.py
scripts/run_comprehensive_ablation.py
scripts/run_retrain_all_rdkit.py
scripts/save_best_params.py
scripts/train_cnn_bilstm_physchem_embedding.py
scripts/train_hybrid_physchem_matrix.py
scripts/ultra_fast_embedding_training.py
scripts/ultra_fast_training.py
scripts/verify_ablation_comprehensive.py
src/ablation_models.py

# Documentation (local / deploy notes — not required for inference)
docs/

# Local tool caches (e.g. matplotlib font cache under repo)
.mplconfig/

# Cursor / local tooling
.cursor/
revision_artifacts/
automation_logs/
# ML experiment trackers
wandb/
mlruns/

# Test / type caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.hypothesis/
revisions/

# Optional local bundles
*_figures_hires.zip
